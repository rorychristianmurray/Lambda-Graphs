
def earliest_ancestor(ancestors, starting_node):
    ## build the graph

    ## traverse the graph
    ## as traverse track path
    ## get length of paths
    ## return root of longest path
    ## if satisfy reqs
